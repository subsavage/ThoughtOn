import 'package:flutter/material.dart';

class ThoughtDetails extends StatelessWidget {
  const ThoughtDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Thought Detialst"),
      ),
    );
  }
}
