import 'package:flutter/material.dart';

class CreateThought extends StatelessWidget {
  const CreateThought({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Create Thought"),
      ),
    );
  }
}
